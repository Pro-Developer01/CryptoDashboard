import { WalletList } from "../Pages";

const navRoutes = [
  {
    path: "walletlist",
    component: WalletList,
  },
  {
    path: "transactions",
    component: WalletList,
  },
];

export default navRoutes;
